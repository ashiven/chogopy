# RUN: lli %s.ll | filecheck %s
# RUN: python %s | filecheck %s

b: bool = True
c: bool = False

b = True and b
c = b and (not c) or b
c = (b == c) or (b != c) if not b else c

print(c)

# CHECK: True
